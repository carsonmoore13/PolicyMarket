{
  "name": "policymarket",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "serve .",
    "dev:backend": "npm run dev --prefix backend",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "serve": "^14.2.5"
  },
  "overrides": {
    "ajv": "^8.18.0",
    "minimatch": ">=3.1.3"
  },
  "dependencies": {
    "mongodb": "^7.1.0"
  }
}
